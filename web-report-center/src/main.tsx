import React from "react";
import ReactDOM from "react-dom/client";
import { ReportCenterApp } from "./reportCenterApp";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ReportCenterApp />
  </React.StrictMode>,
);
